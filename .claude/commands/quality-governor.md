# FlowSight Quality Governor

This Claude Code command is a thin adapter. The canonical, tool-neutral procedure
lives in `docs/workflows/quality-governor.md` so that Codex, Claude Code, and humans all follow
the same steps.

Do this now:

1. Read `docs/workflows/quality-governor.md`.
2. Follow it exactly, using the inputs the user provided.

Do not maintain a separate Claude-only version of this workflow. If the procedure
needs to change, edit `docs/workflows/quality-governor.md`; this file must stay a pointer.
